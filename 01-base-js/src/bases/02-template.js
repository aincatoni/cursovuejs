const nombre = "Ain";
const apellido = "Catoni";

const nombreCompleto = `${nombre} ${apellido}`;
console.log(`Resultado: ${1 + 1}`);

function getSaludos(nombre) {
  return "Hola " + nombre;
}

console.log(`Este es un texto: ${getSaludos(nombre)}`);
